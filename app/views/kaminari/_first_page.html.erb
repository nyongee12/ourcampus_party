<%# Link to the "First" page
  - available local variables
    url:           url to the first page
    current_page:  a page object for the currently displayed page
    total_pages:   total number of pages
    per_page:      number of items to fetch per page
    remote:        data-remote
-%>
<span class="first">
  <% unless current_page.first? %>
    <a href="<%=url%>" class="paginate_arr" data-remote="<%=remote%>">
      <i class="fa fa-angle-double-left" aria-hidden="true" style="font-size: 17px"></i>
    </a>
  <% else %>
    <a href="javascript:void(0);" style="cursor:default" class="paginate_arr" data-remote="<%=remote%>">
      <i class="fa fa-angle-double-left" aria-hidden="true" style="color: #aaa; font-size: 17px"></i>
    </a>
  <% end %>
</span>
